name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run format:check
      - run: npm run lint
      - run: npm run compile

      - run: npm run build
      - run: npm run build:firefox

      - run: npm ci
        working-directory: preview-page
      - run: npm run test
        working-directory: preview-page
      - run: npm run build
        working-directory: preview-page
